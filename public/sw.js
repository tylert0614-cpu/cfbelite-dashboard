self.addEventListener("push",(event)=>{let payload={};try{payload=event.data?event.data.json():{}}catch{payload={body:event.data?.text()||"New CFBElite Social activity"}}const title=payload.title||"CFBElite Social";const options={body:payload.body||"You have a new league notification.",icon:payload.icon||"/cfbelite27-logo.png",badge:payload.badge||"/cfbelite27-logo.png",tag:payload.tag||payload.type||"cfbelite-social",renotify:Boolean(payload.renotify),data:{url:payload.url||payload.target_url||"/",...(payload.data||{})},vibrate:[180,80,180]};event.waitUntil(self.registration.showNotification(title,options));});self.addEventListener("notificationclick",(event)=>{event.notification.close();const url=event.notification.data?.url||"/";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then((windows)=>{const match=windows.find((client)=>client.url.includes(self.location.origin));if(match){match.focus();match.navigate(url);return match}return clients.openWindow(url)}));});